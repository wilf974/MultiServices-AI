# MultiService AI - dependances

# --- noyau (substrat + tests) : pur Python, installe partout ---
pydantic>=2.0
pytest>=8.0
cryptography>=42        # chiffrement au repos + crypto-shredding (multiservice.crypto) : AES-GCM + wrap de cle

# --- backend local GGUF (EmbeddedGGUF) : NE PAS installer via cette ligne ---
# llama-cpp-python s'installe a part, car le wheel depend de ta pile (CPU vs CUDA).
#
#   GPU / CUDA (recommande pour un 12B), wheel pre-compile officiel :
#     pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
#     (remplace cu124 par ta version : cu118 cu121 cu122 cu123 cu124 cu125 cu130 cu132 ;
#      verifie avec `nvidia-smi`)
#
#   CPU (simple, lent sur 12B) :
#     pip install llama-cpp-python
#
# Rappel VRAM : Gemma-4-12B en BF16 ~24 Go > 16 Go VRAM. Prefere un GGUF quantifie
# (Q4/Q5/Q8) ou un offload partiel (--n-gpu-layers). Le code est model-agnostic.
