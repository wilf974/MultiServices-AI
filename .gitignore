# =====================================================================
#  DONNÉES PRIVÉES — NE JAMAIS COMMITTER (contenu de la mémoire/journal)
# =====================================================================
*.jsonl
journal*.jsonl
embeddings*.jsonl
cache*.jsonl
semcache*.jsonl
.aethercore/
logs/
skills/                 # skills promues locales (peuvent refléter du vécu)

# Copie locale du projet compagnon (ne pas publier)
reference/

# Artefacts de travail du workflow SDD (briefs/reports/review-packages) -- local only
.superpowers/

# Coffre Obsidian personnel (notes de terrain, à curer avant toute publication)
MultiService-IA-Vault/

# Docs internes/privés (observations terrain, repère de reprise) — NE PAS publier tel quel
CLAUDE.md
docs/DECISIONS.md

# Scripts perso (host/port/user VPS en dur, coordonnées LAN/SSH) — automatisation locale, JAMAIS publier
scripts/sync_memory_merge.ps1
scripts/sync_memory_scheduled.ps1
scripts/sync_memory_to_vps.ps1
scripts/gpu-tunnel.ps1
scripts/git-commit-*.log
scripts/git-commit-session.ps1

# Artefacts d'outillage local (peuvent contenir des chemins/traces perso)
.claude-flow/

# READMEs vestiges de scaffolding (chemins perso + nom de modèle) — supprimés, ne pas re-committer
README-MCP.md
README-SPRINT13.md

# Modèles locaux (lourds)
*.gguf

# Artefact généré par la démo (fictif, mais regénérable)
examples/memory_demo/demo_events.json

# =====================================================================
#  Python
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# =====================================================================
#  IDE / OS
# =====================================================================
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
